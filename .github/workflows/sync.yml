name: Sync Files
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Run GitHub File Sync
        uses: Redocly/repo-file-sync-action@master
        with:
          GH_PAT: ${{ secrets.PACKAGE_TOKEN }}
          GIT_EMAIL: changmin.yi@myrealtrip.com
          GIT_USERNAME: yichangmin
          PR_LABELS: |
            file-sync
            automerge
